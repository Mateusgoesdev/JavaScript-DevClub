// Escreva um programa onde, voce chame uma função enviando um número, a funçao deve imprimir na tela os números de 1 até o número que voce enviou.

function contagem(numeroMaximo) {
    let numeroMaximo
    for (let i = 0; i < numeroMaximo; i++) {
        console.log(i)
    }
}
console.log(contagem(50))